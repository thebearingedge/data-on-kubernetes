#!/bin/sh

# shellcheck disable=SC2034

LOCAL_HOSTNAME="local.test"
CLOUD_HOSTNAME="cloud.test"

TF_VAR_local_hostname="${LOCAL_HOSTNAME}"
TF_VAR_cloud_hostname="${CLOUD_HOSTNAME}"

S3_ACCESS_KEY_ID="minioadmin"
S3_SECRET_ACCESS_KEY="minioadmin"

LOCAL_CA_CERT="/path/to/local-ca.pem"
LOCAL_CA_KEY="/path/to/local-ca-key.pem"

TF_VAR_local_ca_cert="${LOCAL_CA_CERT}"
TF_VAR_local_ca_key="${LOCAL_CA_KEY}"